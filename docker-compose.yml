version: "3"
services:
  frontend:
    build:
      ./frontend
    ports:
      - "8080:80"
  backend:
    build:
      ./backend
    depends_on:
      - db
    ports:
      - "5000:5000"
  db:
    image: redis:alpine
